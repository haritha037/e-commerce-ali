eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false # For Eureka itself (Discovery server should not register himself)
    fetch-registry: false # Here we are configuring eureka itself
    service-url:
      defaultZone: http://${eureka.instance.hostname}/${server.port}/eureka/

server:
  port: 8761
